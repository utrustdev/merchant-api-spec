get:
  tags:
    - "Order"
  summary: "List orders for all the merchant's stores"
  operationId: listOrders
  parameters:
    - $ref: "#/definitions/Pagination/parameters/cursorParam"
    - $ref: "#/definitions/Pagination/parameters/limitParam"
    - in: query
      name: include
      type: string
      enum: ["payments"]
  responses:
    '200':
      description: List of orders for merchant
      schema:
        type: "object"
        properties:
          data:
            type: "array"
            items:
              - $ref: "#/definitions/Order"
