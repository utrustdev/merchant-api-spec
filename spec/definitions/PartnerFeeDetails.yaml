type: "object"
properties:
  receiver:
    type: "string"
    enum: ["utrust"]
    example: "utrust"
  amount:
    type: "object"
    properties:
      total:
        type: "string"
        format: "decimal"
        example: "0.40"
      currency:
        type: "string"
        example: "USD"
